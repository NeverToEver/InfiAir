# AGENTS.md — 工程纪律

工程质量与流程的唯一判定口径；本文件只写纪律，不复述其余文档的内容。读法：动手前查 §1 路由表，看 §2（我能定什么）与 §3（这类改动要做到什么）；写测试看 §4；提交前照 §6 跑一遍。

## 1 项目形态、单源与路由

Godot 4.7 .NET + C#（零 GDScript），GL Compatibility，1920×1080；平台 PC 桌面，输入面只有键鼠与手柄两路。目录职责见 `README.md`「项目结构」；`csharp/core/` 是纯逻辑层（零 Godot 依赖、可单测），`csharp/godot/` 只做引擎适配，`csharp/tests/` 只测 core 且不入主 sln。

**路由表＝单源表**：一个事实只写一份，别处只给指针——要找什么、要改什么，只去这一个路径。

| 要做的事 | 唯一路径 | 备注 |
| --- | --- | --- |
| 调数值 | `data/balance.json` | 可视化：`python3 scripts/tools/balance_editor.py`（键的说明 / 单位 / 取值范围在 `scripts/tools/balance_meta.json`，权威仍是代码里的钳制）；代码回退默认值不得与它分叉 |
| 改 UI 配色 | `csharp/godot/UITheme.cs` | 场景内嵌的 VFX / 粒子色以对应 `.tscn` 为准 |
| 改玩家可见文案 | `data/translations.csv` | 中英双列；键缺失玩家就会看到键名 |
| 改帧级口径（单帧推进上限等） | `csharp/godot/FrameCache.cs` | |
| 查设计定稿取值 / 改设计口径 | `docs/DESIGN_BASELINE.md` | 纯参考；小节号被代码注释按 `§x.y` 引用 |
| 查为什么这么定、反转链、开放债务 | `docs/ROADMAP.md` | 决策一行制；更早决策在 git 历史 |
| 查行业依据与出处 | `docs/REFERENCES.md` | 决策引用它；它不引用决策 |
| 写发布说明 | `docs/RELEASE_NOTES.md` | `release.sh --publish` 取当前版本节作 Release 正文 |
| 跑门禁 | `python3 scripts/ci/gates.py` | 步骤清单 `--list`；判据口径＝各脚本注释；冒烟帧数＝`check_smoke.sh` |
| 改素材 | `scripts/tools/regenerate_all.sh` 重跑 | 重跑后 `git diff` 必须为空（素材一律程序化生成） |
| 改纪律 / 门禁 / 提交规范 | 本文件 | 改门禁须同提交给破坏验证（§6） |

不变式（写错不报错、只会悄悄坏，改动必须验）：存档读写对称 / 设置读写对称 / balance 键真实存在 / 文案键真实存在 / 事件状态机能跑完全周期 / 存档旧档可读入不丢进度。
政策：**依赖分三种口径**——项目运行时与游戏代码**零第三方库**（Godot 与 .NET 官方、Python 标准库；不引入 GDScript）；**构建期素材生成器允许 Python 第三方库（当前仅 Pillow）**（`scripts/tools/` 是离线工具，产物入库、自身不进发布包、不参与运行时）；**测试设施允许系统级依赖**（CI 与本地门禁可装官方 Godot 引擎与 .NET SDK），但测试设施与构建工具均不得成为运行时依赖、不得进发布包。

## 2 决策权

**可以自主做**：按既有规格实现功能、修 bug、修与规格不符的实现；行为不变的重构、命名与目录整理、删孤儿代码；测试、注释、文档结构；性能优化（不改观感、难度与玩家可感数值）；让门禁真正能失败（修假绿）。
**按依据定稿、不挂起等待裁定**（2026-09-19 人类指令：取消「待人类裁定」，不再区分人工与工程迭代）：`balance.json` 里玩家可感的取值（伤害、血量、速度、冷却、概率、奖励、手感系数）；难度曲线、手感、节奏；新增或删除玩家可见内容与文案；新增设置项、改存档格式、改输入面；平台范围、第三方依赖、发布——这些由实现者给出取值与依据后**直接落地定稿**，不在文档里另立「待确认」「人工验收」环节。
定稿取值须给成对信息（提交正文或 `DESIGN_BASELINE` 对应节）：

```
结论：<取值>（已定稿）
| 选项 | 值 | 实测 / 依据 | 代价 |
```

人类可随时改判任何取值：改值即改对应单源（多在 `balance.json`）一处，反转链随决策条目给出。
判据速记：**玩家能感觉到、且没有客观对错的，交给可查的依据与行业惯例**（出处进 `docs/REFERENCES.md`）；能靠实测与规格判定的，靠实测与规格。

## 3 完成的定义

通用清单（每次改动逐条过）：1 `python3 scripts/ci/gates.py` 全绿，交付说明给命令与实际结果（与人类的一切对话一律简体中文）；2 行为改动有回归面；确实无法自动化的，留代码层护栏（断言、日志、取值钳制）；3 涉及数值 / 文案 / 存档 / 设置 → 对应单源已改，全库无第二处硬编码；4 新增数值键在 `balance.json` 有值且与代码默认一致；5 删改符号无悬空引用（`grep` 旧名）；6 按 §6 拆分与撰写提交。

分类型附加：

| 改动类型 | 额外必做 |
| --- | --- |
| `csharp/core/` 纯逻辑 | 新增/改单测（先红后绿） |
| 引擎耦合行为（节点 / 事件 / 输入 / 结算 / 生成） | 判定可下沉的必须下沉 `core/` 并配单测；纯适配留在 `godot/` 层是正常的 |
| 视觉 / 着色器 / UI 布局 | 几何与布局算式优先下沉 `core/` 单测；观感按准入判据与护栏随批收口 |
| 数值调参 | 键存在 + 取值路径跑通；取值与依据一并定稿（§2） |
| 新增/改动设置项 | 写读对称；改默认值或字段迁移另做窗口化过目 |
| 存档字段 | `csharp/tests` 补一条「旧档缺新字段可读入且不丢已存进度」的用例 |
| 玩家可见文案 | 翻译表键存在 |
| 性能优化 | 无头同场景同帧数墙钟用时前后对比（粗筛；观感退化仍需窗口化过目），退化要写明取舍 |
| 依赖 / 工具链 | 先按 §2 提议 |

## 4 测试策略

选层判据（自上而下取第一个可行）：1 不依赖 Godot 的纯函数 / 值对象（几何、时序、经济、概率、整形、判型）→ `csharp/core/` 单测；2 必须依赖引擎生命周期 / 物理 / 场景树 → 无头固定步长场景 + 完成标记；3 依赖手感 / 长时稳定性 → 代码层护栏 + 依据定稿。
Godot 节点只做适配（取节点、设属性、连信号）：**决策下沉 `core/`，节点不藏判定**；不为下沉而下沉。
确定性硬规则（无头场景一律）：1 不等待真实时间，一律 `--fixed-fps 60`（帧数＝模拟时长）；2 **判定与模拟只看模拟状态**——时长取自 `delta` 累计、帧序取自 `Engine.GetPhysicsFrames` / `Engine.GetProcessFrames`，不拿墙钟、帧率、机器性能当判据；3 随机要么避免、要么走可注入取值源，不得依赖 `GD.Rand*` 默认序列；4 不依赖窗口 / 分辨率 / GPU；5 不依赖外部残留状态（存档先复位，或用临时目录）。
真实时间（墙钟）不是禁用：确有几处天然属真实时间——开机耗时读数、设置页帧率/刷新率读出、音效最小触发间隔、人机输入宽限与手感推进（按玩家物理按键与真实帧长计，不随 `Engine.TimeScale` 缩放）。**划线标准**：问它等待/限制的是「引擎之外的现实世界」（人的手、输入队列、声波、机器耗时）还是「游戏世界内部已推进多少」——前者真实时间，后者模拟时间；同一时序语义严禁两套时钟混用。
完成信号：无头场景跑完必须打固定标记，门禁断言它；只判「不崩」不算覆盖——帧数不够时事件中途停摆同样零错误退出。
不能自动化时：代码层护栏（断言、日志、取值钳制），并说明为何不能自动化。
诚实：修 bug 先写能复现的用例；用例要能说清「实现坏了它怎么红」，说不清就删；不稳定测试修成确定性或删，不许用重试掩盖。

## 5 门禁

本地一把跑完：`python3 scripts/ci/gates.py`——五步主干，判据是**游戏正常路径跑得起来**：

| 步骤 | 判什么 |
| --- | --- |
| `build` | `dotnet build` 零警告（`TreatWarningsAsErrors`） |
| `unit_tests` | `csharp/tests` 全绿，且不能空跑（TRX 用例数 > 0） |
| `tool_tests` | `scripts/tests` 全绿，且不能空跑（用例数 > 0）：数值管理器的形状校验 / 行尾与数值形态、元数据与数值表的对账、分析公式的 C# 算例镜像、预设的路径与结果域、服务器生命周期判定 |
| `import` | 资源导入无错误 |
| `smoke` | 无头固定步长跑生产 `main.tscn`，开机直达标题屏（完成标记 `[boot] 标题屏就绪`） |

`--only <slug>` 只跑子集，`--list` 列出步骤。只跑引擎步（`import` / `smoke`）时**不包含 `build`**——改了 C# 却没带上 `build` 会跑在旧程序集上，判绿也是旧代码的绿。每步判定逻辑在各自脚本里，本文件不复述口径。
铁律：1 **每个门禁必须能失败**——新增或改门禁同提交给破坏验证（怎么破坏、红了什么）；2 **假绿比没门禁更糟**——取不到判据必须显式失败（工具缺失 / 路径不存在 / 零命中）；3 本文件每轮注入上下文，膨胀是静默成本——加规则前先想能不能删掉一条等量的。
新增门禁的准入条件：一句话说清「抓的是哪种静默错误」+ CI 稳定复现 + 失败输出能指向位置；落不到这三条的，写成 §3 检查项，不造脚本。
无头冒烟口径：

```bash
# 生产主场景（开机链路，＝门禁的 smoke 步）
godot --headless --path . --fixed-fps 60 --quit-after <帧数>
# 指定场景（按需手跑，门禁不含；如教程 scenes/tutorial.tscn）
godot --headless --path . --fixed-fps 60 --quit-after <帧数> --scene res://<场景路径>
```

- 帧数＝目标模拟时长秒数 × 60（**单源在 `scripts/ci/check_smoke.sh`**，当前 300 帧＝5 模拟秒）；改场景后按需手跑，长周期行为没有自动判据。
- 主场景趟把 `APPDATA`（Windows）/ `XDG_DATA_HOME`（Linux）/ `HOME`（macOS）都指向本趟独立临时目录，并断言目录里确实出现引擎写出的 `user://` 日志——隔离失效时会读走开发者配置、写坏真实存档，而判定照常通过。
- 引擎错误正则含 `Invalid polygon data` 与通用 `ERROR:`：前者是程序化绘制的静默坏点——程序化绘制在 headless 下仍会执行 `_Draw`，自交/退化多边形在三角化处报该错并整块不画——不崩、只看退出码抓不到。
- 等真实时间窗（如标题屏 0.5s 输入守卫，实现是墙钟时间戳比较）别逐帧空等：无头 fixed-fps 下逐帧空等按墙钟烧帧（实测 0.5s 守卫约 200–300 帧）——墙钟照走，帧预算不跟着涨。计时点用哪套时钟按 §4 的划线标准。

## 6 提交与发布

粒度：一个逻辑一个提交；纯格式化、重排、删除独立成提交；纪律与门禁改动单独成提交，便于单独回滚。
主题：≤72 字，`类型: 是什么`；类型限 `feat / fix / refactor / perf / test / docs / chore`。
正文：只写「为什么」与关键取舍，≤3 行；推翻既有决策必须写明；不抄 diff、不复述验证流水、不写日期与轮次编号。
先落文档再动手：存档格式、设置键、输入面、事件口径、平台范围、以及本文件本身。
提交前：

```bash
python3 scripts/ci/gates.py          # 全绿
git status --porcelain               # 只应包含本次改动
git show --stat HEAD                 # 提交内容与预期一致
```

发布：`./release.sh` 导出并打包到 `builds/release/`；`./release.sh --publish` 追加 tag 与 GitHub Release。
反悔留痕：推翻既有决策时加一条新决策条目，不改旧条目。

## 7 文档

六份活跃文档，各司其职、只给指针不复述：`README.md` / `README_EN.md`（怎么跑、怎么玩）、本文件（纪律与门禁）、`docs/DESIGN_BASELINE.md`（设计定稿与取值，纯参考）、`docs/ROADMAP.md`（方向 + 开放项 + 当前周期决策）、`docs/REFERENCES.md`（外部依据与出处）、`docs/RELEASE_NOTES.md`（Release 正文单源）。**找哪份看 §1 路由表。**
条目格式：开放项 `- **[影响面] 是什么**：为什么不现在做；收口条件＝怎么算做完；触发时机＝什么时候做。`；决策条目 `- **日期 决策**：为什么 + 反转链。`（一行制）。
活跃文档只写「现状 + 开放项」；根因、修法、验证过程属 git 历史；修完直接删条目，不写「已修复」回顾；要删的文档直接删（原文可从 git 历史恢复），不做「只读存档」形态（依据见 `docs/REFERENCES.md` §4.13）。

## 8 注释、命名、术语与语言

注释只写代码表达不出的东西：为什么这么做、边界条件、坑、外部约束（引擎行为、平台差异）；不写变更史与日期、审计轮次、复述代码的「是什么」、无主 TODO（要么写清触发条件与收口，要么删）。
命名：类型与公开成员 `PascalCase`、局部与参数 `camelCase`、私有字段 `_camelCase`、常量 `PascalCase`；数值键 `snake_case`；文件名与类型同名。
术语单一叫法，新术语先写进下表再用：

| 用 | 不用 |
| --- | --- |
| 本局（合法复合词：本局内） | 对局 / 单局 / 局内 |
| 敌机 / 敌弹 | 敌人 |
| 增幅 | buff / 增益 |
| 天赋 | 技能树 |
| 弹反 | 格挡 |
| 弹体（通用弹；按阵营写「玩家弹 / 母舰弹」） | 弹丸（语义分裂，勿用） |
| 键鼠 / 手柄（仅有的两路输入面） | 触屏 / 触控 / 移动端（已在代码层退役，回归须先推翻决策） |
| 黎明站（地点专名）/ 基地（功能语境） | — |

语言：散文（注释 / 文档 / 提交信息）与面向人类的对话（进度说明 / 交付说明 / 结论 / 提问）一律简体中文；标识符、类型名、引擎 API、配置路径、动作名、外部产品名保留英文。
玩家文案：只从 `data/translations.csv` 出；不出现开发措辞与占位符。
脚本：变量紧邻全角字符时必须写 `${name}`——`$name（…）` 里全角括号会被并入变量名，轻则报错消息印不出取值，重则 `set -u` 下直接中止（本仓库脚本与命令里中文随处可见，这条是真会踩的）。

## 9 已知偏离与例外

偏离纪律必须登记在 `docs/ROADMAP.md`「已知债务与开放发现」（格式：是什么 / 为什么不现在做 / 收口条件 / 触发时机），口头不算。现有条目一律以 ROADMAP 为准，本节不列表。

## 维护本文件

改纪律 = 改本文件 + 同步受影响的门禁脚本 + 同一个提交。新增规则必须能落到 §3 清单或 §5 门禁；落不到判定的不要写。
