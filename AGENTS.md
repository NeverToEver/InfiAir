# AGENTS.md — 工程纪律

工程质量与流程的唯一判定口径；本文件只写纪律，不复述其余三份的内容。做什么见 `README.md`，方向与开放项见 `docs/ROADMAP.md`，设计意图见 `docs/DESIGN_BASELINE.md`。
读法：动手前看 §3（我能定什么）与 §4（这类改动要做到什么）；写测试看 §5；加设施或改门禁看 §6；提交前照 §7 命令序列跑一遍。

## 1 项目形态与不变式

Godot 4.7 .NET + C#（零 GDScript），GL Compatibility，1920×1080；平台 PC 桌面，输入面只有键鼠与手柄两路。
目录职责：`csharp/core/` 纯逻辑（零 Godot 依赖，可单测）；`csharp/godot/` 引擎适配层（场景脚本、UI、服务）；`csharp/tests/` 只测 core，不入主 sln（构建发布不耦合测试工具链）；`scenes/` 场景；`data/` 数值与翻译表；`scripts/ci/` 门禁；`scripts/tools/` 素材生成器与数值编辑器。

单源（一个事实只写一份，别处给指针）：

| 事实 | 唯一位置 |
| --- | --- |
| 数值 | `data/balance.json` |
| 颜色 | `csharp/godot/UITheme.cs` |
| 玩家可见文案 | `data/translations.csv` |
| 帧级口径（单帧推进上限等） | `csharp/godot/FrameCache.cs` |
| 门禁口径与命令 | 本文件 §6 |
| 冒烟帧数 | `scripts/ci/check_smoke.sh` |
| 真实时间允许清单 | `scripts/ci/check_realtime_allowlist.sh`（脚本内 `ALLOW` 表） |

不变式（写错不报错、只会悄悄坏，改动必须验）：存档读写对称 / 设置读写对称 / balance 键真实存在 / 文案键真实存在 / 事件状态机能跑完全周期 / 存档旧档可读入不丢进度。
政策：**依赖分三种口径**——项目运行时与游戏代码**零第三方库**（Godot 与 .NET 官方、Python 标准库；素材一律程序化生成，`scripts/tools/regenerate_all.sh` 重跑后 `git diff` 应为空；不引入 GDScript）；**构建期素材生成器允许 Python 第三方库（当前仅 Pillow）**（`scripts/tools/` 是离线工具，产物入库、自身不进发布包、不参与运行时）；**测试设施允许系统级依赖**（CI 与本地门禁可装系统包，如视觉门禁的 Xvfb + Mesa 软光栅、官方 Godot 引擎与 .NET SDK），但测试设施与构建工具均不得成为运行时依赖、不得进发布包。

## 2 取舍顺序

顺序即纪律：1 **正确性**——真实路径跑得通、可重复验证；2 **回归面**——留下的自动判定，胜过一次性人工过目；3 **一致性**——命名、术语、结构统一；4 **卫生**——散文形态，不产生质量信号。
三条裁决例：门禁与正确性冲突→先改门禁判据，不绕开（判不出你要做的事，先改判据）；样式一致与可测性冲突→让位可测性（判定挪进 `core/` 会打破「就近放置」，仍然挪）；确实无法自动化→不降标准，**登记人工项 + 留代码层护栏**（§5）。

## 3 决策权

**可以自主做**：按既有规格实现功能、修 bug、修与规格不符的实现；行为不变的重构、命名与目录整理、删孤儿代码；测试、注释、文档结构；性能优化（不改观感、难度与玩家可感数值）；让门禁真正能失败（修假绿）。
**只能提议、不许擅自定**（属人）：`balance.json` 里玩家可感的取值（伤害、血量、速度、冷却、概率、奖励、手感系数）；难度曲线、手感、节奏；新增或删除玩家可见内容与文案；新增设置项、改存档格式、改输入面；平台范围、第三方依赖、发布。
提议须给成对信息，落地时在提交正文标注「该取值待人类确认」：

```
结论：建议 <取值>（默认已按它落地，待确认）
| 选项 | 值 | 实测 / 依据 | 代价 |
```

人类没答复时：保留默认值，在 `docs/ROADMAP.md` 开放项写一行「待确认：<取值>；收口条件＝人类认可或改值」；不得把未确认取值当成既成事实写进 `DESIGN_BASELINE.md`。
判据速记：**玩家能感觉到、且没有客观对错的，归人**；能靠实测与规格判定的，归实现者。

## 4 完成的定义

通用清单（每次改动逐条过）：1 `python3 scripts/ci/gates.py` 全绿，交付说明给命令与实际结果（与人类的一切对话一律简体中文）；2 行为改动有**能失败的**回归面，否则按 §5 登记人工项 + 护栏；3 涉及数值 / 文案 / 存档 / 设置→对应单源已改，全库无第二处硬编码；4 新增数值键在 `balance.json` 有值且与代码默认一致；5 删改符号无悬空引用（`grep` 旧名）；6 按 §7 拆分与撰写提交，未做完的登记开放项（§8）。

分类型附加：

| 改动类型 | 额外必做 |
| --- | --- |
| `csharp/core/` 纯逻辑 | 新增/改单测（先红后绿） |
| 引擎耦合行为（节点 / 事件 / 输入 / 结算 / 生成） | 相关无头场景覆盖该路径并断完成标记；判定可下沉的必须下沉 `core/` 并配单测 |
| 视觉 / 着色器 / UI 布局 | 几何与布局算式优先下沉 `core/` 单测；`check_visual.sh` 截图探针兜住渲染整体坏掉/页面切换失效；观感与细微退化窗口化人工过目（登记人工项，写清怎么算过） |
| 数值调参 | 键存在 + 取值路径冒烟；玩家可感的手感值另登记人工项 |
| 新增/改动设置项 | `check_settings_symmetry.sh`（写读对称 + 文案键）+ 设置页开页冒烟；改默认值或字段迁移另做窗口化过目 |
| 存档字段 | `check_save_symmetry.sh` + `csharp/tests` 补一条「旧档缺新字段可读入且不丢已存进度」的用例 |
| 玩家可见文案 | `check_ui_copy.sh` + 翻译表键存在 |
| 遭遇事件 / 波次 | 对应 `--event-probe` 全周期 + 完成标记 |
| 性能优化 | 无头同场景同帧数墙钟用时前后对比（粗筛；观感退化仍需窗口化过目），退化要写明取舍 |
| 依赖 / 工具链 | 先按 §3 提议；运行时零第三方库、构建期素材生成器可用 Pillow、测试设施可装系统包（口径见 §1） |

## 5 测试策略

选层判据（自上而下取第一个可行）：1 不依赖 Godot 的纯函数 / 值对象（几何、时序、经济、概率、整形、判型）→`csharp/core/` 单测；2 必须依赖引擎生命周期 / 物理 / 场景树→无头固定步长场景（`--event-probe` 这类探针）；3 依赖手感 / 长时稳定性→人工过目 + 登记人工项。视觉同理：**判定优先下沉 `core/` 单测**（几何取值、布局算式），截图探针只作辅助（生成截图 + 抓渲染整体坏掉）。
Godot 节点只做适配（取节点、设属性、连信号）：**决策下沉 `core/`，节点不藏判定**；不为下沉而下沉——纯适配留在 `godot/` 层是正常的。引擎 / 渲染器版本变更的「观感等价」另走同机 A/B：`scripts/tools/shot_ab.py`（同机同后端、只差引擎版本才可比；判据是**逐页噪声基线 + 边缘归因**，不是像素基线——基线本身超限即判判据不可用）；它不是 CI 门禁（需要两个引擎），属升级流程的一步。
确定性硬规则（无头场景与探针一律）：1 不等待真实时间，一律 `--fixed-fps 60`（帧数＝模拟时长）；2 **判定与模拟只看模拟状态**——时长取自 `delta` 累计、帧序取自 `Engine.GetPhysicsFrames` / `Engine.GetProcessFrames`，不拿墙钟、帧率、机器性能当判据；3 随机要么避免、要么走可注入取值源，不得依赖 `GD.Rand*` 默认序列；4 不依赖窗口 / 分辨率 / GPU；5 不依赖外部残留状态（存档先复位，或用临时目录）。遭遇 / 手感 / 长局探针一律关闭迷雾随机事件（首延迟过后每 3s 掷签，探针红绿会取决于随机数），迷雾由 `--fog-probe` 专趟覆盖、仍走生产资格与门槛。**视觉截图趟例外于第 1、4 条**（需真实渲染器与固定分辨率），其确定性由容差比对保证（画面含墙钟脉冲，不可逐像素比）。
真实时间（墙钟）不是禁用，但**必须逐点登记**（清单单源在 `check_realtime_allowlist.sh` 的 `ALLOW` 表）。第 2 条约束的是**判定与模拟**；模拟与判定之外，确有几处量天然属真实时间：开机耗时读数、设置页帧率/刷新率读出、音效最小触发间隔（混音按真实时间轴推进，暂停/子弹时间下模拟时间会停摆或被缩放）、人机输入宽限与手感推进（按玩家物理按键与真实帧长计，不随 `Engine.TimeScale` 缩放）。判定新计时点用哪套时钟的划线标准：**问它等待/限制的是「引擎之外的现实世界」（人的手、输入队列、声波、机器耗时）还是「游戏世界内部已推进多少」——前者真实时间，后者模拟时间。** 同一时序语义严禁两套时钟混用；间距/宽限这类以人类秒为单位的常数，通常就是真实时间的信号。
完成信号：无头场景跑完必须打固定标记（如 `[event-probe] <id> 全周期完成`），门禁断言它；只判「不崩」不算覆盖——帧数不够时事件中途停摆同样零错误退出。
不能自动化时：登记人工项（§8 格式，写清「怎么算过」）+ 代码层护栏（断言、日志、取值钳制），并说明为何不能自动化。
顺序与诚实：先写能失败的用例再改实现；修 bug 先写复现用例；用例要能说清「实现坏了它怎么红」，说不清就删；不稳定测试修成确定性或删，不许用重试掩盖。

## 6 门禁

本地一把跑完：`python3 scripts/ci/gates.py`（只做调度，判定逻辑在各自脚本里，不复制口径）；`--only <slug>` 只跑子集，`--list` 列出步骤。**各步判据的实现细节与「为什么这样判」写在该步脚本 / 探针源码的注释里，本节只写口径与命令。**
**质量门禁**（判静默错误，是完成依据）：

| 命令 | 判什么 |
| --- | --- |
| `dotnet build` | 零警告（TreatWarningsAsErrors；该属性与 `InfiAir.sln` 主工程 Release 映射由 `check_gate_wiring.sh` 断言——改掉一两行配置就能把「零警告」静默降级成「零 error」） |
| `bash scripts/ci/check_unit_tests.sh` | `csharp/core/` 纯逻辑单测 |
| `bash scripts/ci/check_balance_keys.sh` | 代码读的 balance 键真实存在（防键名写错静默回退默认值）；只认完整点分路径，不做后缀放宽（后缀命中的前缀写错正是要抓的静默错误）；未登记的转发包装器按「实参里出现点分路径字面量」识别（只看名字含 cfg 的包装器，改名即可绕过） |
| `bash scripts/ci/check_balance_dead_keys.sh` | 反向（正向见上一行）：每个叶子键都有读取证据——字面量（`"a.b.c"` 出现在 `csharp/`）/ 动态键族（键模板 + 从代码单源展开出的键集，族内写错的键仍判红）/ 整表读取（容器被 `Cfg` 整取后按键迭代，逐条登记）。无人读的死配置与键名写错判红；键族空转、整表登记指向不存在的路径、叶子键总数低于下限即红 |
| `bash scripts/ci/check_code_defaults.sh` | 代码内缺档回退默认值 == balance 定稿值（防手抄分叉：json 缺失/损坏时静默用错值，编译/单测/冒烟都抓不到）；登记表逐张声明预期条目数，取不到判据（文件/符号/键缺、条目数不符、零比对）即红 |
| `bash scripts/ci/check_save_symmetry.sh` | 存档写读字段一一对应（防进度静默丢失）；键集为空即红（防正则/结构漂移造成假绿） |
| `bash scripts/ci/check_settings_symmetry.sh` | 设置写读字段一一对应 + 设置页文案键存在；键集为空即红 |
| `bash scripts/ci/check_ui_copy.sh` | 玩家可见文案无缺键、无开发措辞、无空字段 |
| `bash scripts/ci/check_realtime_allowlist.sh` | `csharp/` 里的墙钟 / 帧率 / 机器性能读数全部登记在脚本内 `ALLOW`（单源）：未登记的新增判红，**已登记站点消失也判红**（防合法墙钟被「顺手改成模拟时间」）；总命中为零判红；`csharp/core/` 出现任何命中即红 |
| `bash scripts/ci/check_zero_ref_members.sh` | `csharp/` 里「已声明但全库无引用」的公开/内部成员全部登记在脚本内 `ALLOW`（单源，逐条写保留理由）：未登记的新增判红（死代码），**已登记项不再是零引用候选也判红**（合法保留被顺手删掉、或已恢复引用）；候选数低于下限即红。引擎回调/虚表实现、`[Signal]` 委托、`[Export]` 属性、条件编译段、类型声明按类别排除并各自给出理由（不靠放宽规则整类漏掉） |
| `bash scripts/ci/check_import.sh` | 资源导入退出码为 0 且日志除**已登记的退出期噪声**外无 `ERROR`（导入静默坏不改退出码，只在日志留 ERROR）。噪声＝引擎在 **android 导出组**设置上打的「EditorSettings not instantiated yet」那条，且仅当模板逐字相同、同次导入再无其它 ERROR 时放行（其它设置组的同款消息、模板不逐字、噪声与真错误混排一律判红）；**不得改成「再导入一次即干净」**——导入失败会把该资源记进 `.import` 的 `valid=false`，第二次不再报真错误 |
| `bash scripts/ci/check_gate_wiring.sh` | 门禁自身接线完整：`scripts/ci/` 下每个脚本（`.sh` 与 `.py`）都注册进 `gates.py` 与 `ci.yml`（双向——未注册的脚本永不执行，注册了不存在的脚本则 CI 报路径错）；冒烟趟数有**下限**（低于当前趟数即红，防整趟被删）；`run_case` 的日志/用户目录、`expect_marker` 的标记与开关**三方同源绑定**，且两条语句必须是趟函数的**顶层语句**（包进 `if` 就永不执行，只判「文本紧邻」时该趟会静默降级为「不崩即过」）；标记文本含变量（判不出打印点）即显式失败；`ci.yml` 的门禁步骤不得带 `continue-on-error` / `if:` / `working-directory`（否则「CI 绿」与「本地绿」含义分叉），且**装引擎处必须断言版本与 `env.GODOT_VERSION` 同源**（缓存污染或镜像换版会让全部门禁跑到别的引擎上）；`export_presets.cfg` 的 include/exclude 过滤项必须指向仓库内真实存在的路径（写错或漏跟改名 = 该条排除静默失效），且探针源码/场景/测试树必须**全部被 exclude 覆盖**（反向判据：新增 `ProbeHost.*.cs` 分部文件不改 exclude 即红）；`regenerate_all.sh` 必须调用 `scripts/tools/` 下的每个生成器（漏挂 = 该产物永不被重写，而素材门禁照判绿） |
| `bash scripts/ci/check_smoke.sh` | 无头冒烟二十五趟（趟清单见下，判据单源在脚本头注释）；每趟三判——退出码、引擎错误正则、完成标记，缺一不可 |
| `bash scripts/ci/check_visual.sh` | 截图探针（辅助）：固定帧捕获**十张**（HUD、五张设置页、死亡结算页、练习面板、练习局开局、标题屏入口聚焦态），探针自检「画面非空白 + 各图两两互不相同」，抓渲染整体坏掉/页面切换失效；与冒烟同款用户目录隔离。**不是像素级回归**——占位内容（帧率读出、背景动画）随环境变化，细粒度退化归单测与人工。错误行判定先落文件再判（`grep -q` 管道在错误行上千时会因 SIGPIPE 把「有错」判成「无错」）。本步非 headless（dummy 截不到画面），耗时随引擎版本与机器负载变化，**单条可能超 30 秒** |
| `bash scripts/ci/check_assets_reproducible.sh` | **单条超 30 秒**：判「生成器确定」「产物与生成器同步」「**产物来源登记**」——`assets/`+`data/` 下每个被跟踪的非 `*.import` 文件都要在脚本内登记表里按来源分类（生成物 / 手写源 / 第三方 / 引擎伴随文件），**未登记的新增、登记项失效、登记为生成物却没被本次重跑刷新**三种形态都判红。跨机逐字节一致对这条管线做不到（4× 超采样 + LANCZOS/高斯滤波的卷积实现随 Pillow 构建与浮点路径变化），判据是：尺寸/模式相同、PNG **非像素块**（文本/时间/物理分辨率等元数据）相同（时间戳类漂移在此判红）、像素差异占比 ≤ 抗锯齿包络（10%）；出现残差则再重跑一次比对两次输出是否逐字节一致——不一致即「生成器非确定」判红（判据与平台无关），一致则放行并打印残差摘要与「产物同步未在本机判定」的显式声明（不静默放过）。基线不干净（产物路径已有未提交改动）即判据取不到，判红；Pillow 缺失显式判红不跳过。无法用静态扫描替代 |

**卫生门禁**（判散文形态，不产生质量信号）：`bash scripts/ci/check_prose_hygiene.sh` 判注释无日期戳、注释散文简体中文、术语单一叫法（与 §9 表同源）；注释抽取含行尾注释（剥离字符串与 `res://` 协议分隔后取 `//` 之后文本）。
**铁律**：1 **每个门禁必须能失败**——新增或改门禁同提交给破坏验证（怎么破坏、红了什么）；2 **假绿比没门禁更糟**——取不到判据必须显式失败（工具缺失 / 路径不存在 / 零命中）；3 **卫生门禁不得当完成依据**，也不许以「业务优先」长期纵容变红——红了顺手改；4 **时间预算**：本地全跑 ≤200 秒（`gates.py` 结束打印用时；本机空载实测 154 秒＝素材可复现性 83（入库产物与本机栅格化同步的单趟路径）+ 冒烟 25 趟 30（4 路并行）+ 截图探针 26 + 其余 12 步静态判定与构建 15，余量约 46 秒）。新增步之前先想清楚能不能并进既有步或静态化，或先量一遍是哪一步在吃时间；单条超 30 秒在上表注明理由。**素材那步有两条路径**：产物与本机同步时单趟 83 秒，出现残差则退化为双趟重跑（约 160 秒、全跑超预算），须把重跑产物同步入库后恢复——不是「超预算即放宽」，判据强度不换时间。`gates.py` 给每步设墙钟上限（超时判该步失败并继续跑后续步骤），`check_smoke.sh` 给每趟设上限（挂死不得变成无限等待）。**同机并行不受这条约束也不该按它判**：多份门禁同时跑（多个 worktree、或本地与 CI 并行）实测到 250–435 秒，那是负载不是退化。
**新增或改门禁的准入条件**：一句话说清「抓的是哪种静默错误」+ CI 稳定复现 + 失败输出能指向位置 + 有破坏验证；否则写成 §4 检查项或人工项，不造脚本。
**无头冒烟与探针口径**：

```bash
# 生产 main 那趟（开机链路）
godot --headless --path . --fixed-fps 60 --quit-after <帧数>
# 探针各趟（宿主场景 scenes/probe_host.tscn，开关与驱动都在其中）
godot --headless --path . --fixed-fps 60 --quit-after <帧数> --scene res://scenes/probe_host.tscn -- <开关>
```

- 开关与驱动全在探针宿主 `csharp/godot/ProbeHost.cs`（分部文件 `ProbeHost.*.cs`）；生产 `main.tscn`/`Main` 不读任何测试开关（§5「测试设施不进生产路径」）。帧数＝事件全周期秒数 × 60 + 余量；**趟清单、逐趟帧数与日志面的单源在 `scripts/ci/check_smoke.sh`**（调度＝文件末尾 `SMOKE_CASES`），探针断言的细节在 `ProbeHost*.cs` 的注释里——本节不复述清单。
- 二十五趟（执行顺序即 `SMOKE_CASES` 顺序；每趟「`run_case` 紧随 `expect_marker`」成对，由 `check_gate_wiring.sh` 静态判定）：主场景 300 帧（开机链落到标题屏）/ 设置页 / 编队遭遇 / 精英炮塔 / 精英炮塔死亡打断 / 燃料满扫 / 手感复位 / 难度曲线 / 迷雾全周期 / 迷雾打断 / 返航宽限 / 恶意存档 / 死亡删档门控 / Boss 阶段机 / 母舰坞态 / 遭遇击杀型 / 增幅缓存复用 / 存档还原 / 设置版本回退 / 提前离舰 / 教程场景 / 教程全周期 / 练习直选与不落盘 / 本局记录两局语义 / 自动游玩全周期。
- 开关一览（逐条断言见 `ProbeHost*.cs` 注释，帧数见 `check_smoke.sh` 各趟）：`--settings-probe`、`--event-probe=<遭遇 id>`、`--event-probe-death=<id>`、`--event-probe-killall`、`--fuel-probe`、`--feel-probe`、`--long-probe`、`--fog-probe`、`--fog-interrupt-probe`、`--return-probe`、`--hostile-save-probe`、`--save-restore-probe`、`--settings-version-probe`、`--early-leave-probe`、`--death-gate-probe`、`--augment-cache-probe`、`--boss-probe`、`--dock-probe`、`--tutorial-probe`、`--practice-probe`、`--best-record-probe`、`--autoplay-probe[=<秒>]`；辅助探针 `--shot-probe`（截图）、`--startup-time`（观测）。
- 请求遭遇（含击杀型收尾）/ 迷雾 / 返航 / Boss / 坞态的探针一律走生产触发链——资格、门槛、门控由生产判定，探针不得绕过；`--autoplay-probe` 按真实规则玩（不注入无敌、不直接改血量/位置/得分，死了照常记录）。会真跑一局、可能覆写或删掉开发者检查点与设置的两趟（`--tutorial-probe` 写检查点与改键，`--autoplay-probe` 跑整局且可能死亡删档）在未带 `--expect-user-dir` 时**拒绝启动**。
- CI 只跑自动游玩短局（180 模拟秒，判「能开局但玩不起来」）；长局（`=900`）不进 CI，按需手跑（跑法与判据见 `ProbeHost.Autoplay.cs`）——CI 覆盖不到的长时面必须登记（§5 人工项 / ROADMAP 开放项），不得只靠口头。
- 完成标记：每趟跑完必须打固定标记（如 `[event-probe] <id> 全周期完成`），门禁断言它；只判「不崩」不算覆盖——帧数只是上限，事件中途停摆同样零错误退出。标记与开关的对应关系由 `check_gate_wiring.sh` 三方绑定。
- 每趟另带 `--expect-user-dir=<绝对路径>`：探针比对引擎实际用户目录是否落在该隔离根内，不符即报错。`check_smoke.sh` 把 `APPDATA`（Windows）/ `XDG_DATA_HOME`（Linux）/ `HOME`（macOS）都指向该趟的独立临时目录（`<LOG>.userdata`），并断言目录里确实出现引擎写出的 `user://` 日志——隔离失效时恶意档趟会覆写、**死亡趟会删掉开发者的真实检查点**，而所有判定照常通过；截图探针同样隔离（它不用 headless）。
- 引擎错误正则含 `Invalid polygon data` 与通用 `ERROR:`：前者是程序化绘制的静默坏点——程序化绘制 headless 仍会执行 `_Draw`（dummy 渲染不拦绘制调用），自交/退化多边形在三角化处报该错并整块不画——不崩、只看退出码抓不到；退出期资源统计噪声（`N resources still in use at exit`）走白名单。
- 截图探针趟**不用 headless**（dummy 截不到画面）：`--shot-probe` 在固定帧捕获视口，Linux 无 DISPLAY 时脚本自动走 `xvfb-run` + `LIBGL_ALWAYS_SOFTWARE=1`（软光栅）；判定全在探针内，门禁只断完成标记；`KEEP_SHOTS=<dir>` 可把 PNG 留出当截图用。
- 等真实时间窗（如标题屏 0.5s 输入守卫）用 `DelayMsec`：无头 fixed-fps 下逐帧空等按墙钟烧帧（实测 0.5s 守卫约 200–300 帧，`DelayMsec(16)` 约 37 帧）——墙钟照走，帧预算不跟着涨。
- 观测开关（不成趟、不是完成依据）：`--startup-time` 打印启动分段耗时。
- 遭遇事件改动后至少手跑一次对应探针。

## 7 提交与发布

粒度：一个逻辑一个提交；纯格式化、重排、删除独立成提交；纪律与门禁改动单独成提交，便于单独回滚。
主题：≤72 字，`类型: 是什么`；类型限 `feat / fix / refactor / perf / test / docs / chore`。
正文：只写「为什么」与关键取舍，≤3 行；推翻既有决策必须写明；不抄 diff、不复述验证流水、不写日期与轮次编号。
先落文档再动手：存档格式、设置键、输入面、事件口径、平台范围、以及本文件本身。
提交前：

```bash
python3 scripts/ci/gates.py          # 全绿
git status --porcelain               # 只应包含本次改动
git show --stat HEAD                 # 提交内容与预期一致
```

发布：`./release.sh` 导出并打包到 `builds/release/`；`./release.sh --publish` 追加 tag 与 GitHub Release；发布前 `docs/ROADMAP.md`「发布前人工验收」必须清零，否则不发布。
反悔留痕：推翻既有决策时加一条新决策条目，不改旧条目。

## 8 文档

四份纪律文档各司其职，只给指针不复述：`README.md`（怎么跑、怎么发）、本文件（纪律与门禁）、`docs/ROADMAP.md`（方向 + 开放债务 + 人工验收项）、`docs/DESIGN_BASELINE.md`（设计意图定稿）。另有三份非纪律存档：`docs/BALANCE_REVIEW.md`（数值审查存档）、`docs/RELEASE_NOTES.md`（发布说明）、`docs/REFERENCES.md`（公开素材与许可证、行业实践调研存档）。
条目格式：开放项 `- **[影响面] 是什么**：为什么不现在做；收口条件＝怎么算做完；触发时机＝什么时候做。`；人工验收项 `- **名字**：怎么操作、怎么算过；关联代码路径给指针。`；决策条目 `- **日期 决策**：为什么（一行）+ 反转链。`
活跃文档只写「现状 + 开放项」；根因、修法、验证过程属 git 历史；修完直接删条目，不写「已修复」回顾。

## 9 注释、命名、术语与语言

注释只写代码表达不出的东西：为什么这么做、边界条件、坑、外部约束（引擎行为、平台差异）；不写变更史与日期、审计轮次、复述代码的「是什么」、无主 TODO（要么写清触发条件与收口，要么删）。
命名：类型与公开成员 `PascalCase`、局部与参数 `camelCase`、私有字段 `_camelCase`、常量 `PascalCase`；数值键 `snake_case`；文件名与类型同名。
术语单一叫法，新术语先写进下表再用（下表是 `check_prose_hygiene.sh` 的机器副本，**改表必须同步脚本**）：

| 用 | 不用 |
| --- | --- |
| 本局（合法复合词：本局内） | 对局 / 单局 / 局内 |
| 敌机 / 敌弹 | 敌人 |
| 增幅 | buff / 增益 |
| 天赋 | 技能树 |
| 弹反 | 格挡 |
| 弹体（通用弹；按阵营写「玩家弹 / 母舰弹」） | 弹丸（语义分裂，勿用） |
| 键鼠 / 手柄（仅有的两路输入面） | 触屏 / 触控 / 移动端（已在代码层退役，回归须先推翻决策） |
| 黎明站（地点专名）/ 基地（功能语境） | — |

语言：散文（注释 / 文档 / 提交信息）与面向人类的对话（进度说明 / 交付说明 / 结论 / 提问）一律简体中文；标识符、类型名、引擎 API、配置路径、动作名、外部产品名保留英文。对话不入库故门禁判不到，属人工核验项（落 §4 通用清单 1）。
玩家文案：只从 `data/translations.csv` 出，经 `check_ui_copy.sh` 判定；不出现开发措辞与占位符。
脚本：变量紧邻全角字符时必须写 `${name}`——`$name（…）` 里全角括号会被并入变量名，轻则报错消息印不出取值，重则 `set -u` 下直接中止（本仓库脚本与命令里中文随处可见，这条是真会踩的）。

## 10 已知偏离与例外

偏离纪律必须登记在 `docs/ROADMAP.md`「已知债务与开放发现」（格式：是什么 / 为什么不现在做 / 收口条件 / 触发时机），口头不算。需登记：测试或探测设施进生产路径（违反 §5）；门禁判据与声称不符或长期跑不起来（违反 §6）；用人工项替代本可自动化的回归面（违反 §4、§5）；绕过单源（同一事实两处维护）；跨面改动没先落文档（违反 §7）。
本节不列表条目——现状一律以 ROADMAP 为准，避免同一事实两处维护。

## 维护本文件

改纪律 = 改本文件 + 同步受影响的门禁脚本 + 同一个提交。新增规则必须能落到 §4 清单或 §6 门禁；落不到判定的不要写。
