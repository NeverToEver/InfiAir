# AGENTS.md — 工程纪律

工程质量与流程的唯一判定口径。做什么见 `README.md`，方向与开放项见 `docs/ROADMAP.md`，
设计意图见 `docs/DESIGN_BASELINE.md`——本文件只写纪律，不复述它们的内容。

**读法**：动手前看 §3（我能定什么）与 §4（这类改动要做到什么）；写测试看 §5；
加设施或改门禁看 §6；提交前照 §7 的命令序列跑一遍。

参照的通行做法：测试金字塔与 humble object、Conventional Commits、ADR、trunk-based、
零警告构建、确定性 CI。取用它们是为了让判定可重复，不是为了仪式。

## 1 项目形态与不变式

- Godot 4.6 .NET + C#（零 GDScript），GL Compatibility，1920×1080；平台 PC 桌面，
  输入面只有键鼠与手柄两路。
- 目录职责：`csharp/core/` 纯逻辑（零 Godot 依赖，可单测）；`csharp/godot/` 引擎适配层
  （场景脚本、UI、服务）；`csharp/tests/` 只测 core（不入主 sln，游戏构建与发布不耦合测试工具链）；
  `scenes/` 场景；`data/` 数值与翻译表；`scripts/ci/` 门禁；`scripts/tools/` 素材生成器与数值编辑器。
- 单源（一个事实只写一份，别处给指针）：

  | 事实 | 唯一位置 |
  | --- | --- |
  | 数值 | `data/balance.json` |
  | 颜色 | `csharp/godot/UITheme.cs` |
  | 玩家可见文案 | `data/translations.csv` |
  | 帧级口径（单帧推进上限等） | `csharp/godot/FrameCache.cs` |
  | 门禁口径与命令 | 本文件 §6 |
  | 冒烟帧数 | `scripts/ci/check_smoke.sh` |

- 不变式（这些写错了不报错、只会悄悄坏，改动必须验）：
  存档读写对称 / 设置读写对称 / balance 键真实存在 / 文案键真实存在 /
  事件状态机能跑完全周期 / 存档旧档可读入不丢进度。
- 政策：**零第三方依赖**（Godot 与 .NET 官方、Python 标准库；CI 只用官方 action 与官方安装脚本）；
  素材一律程序化生成（`scripts/tools/regenerate_all.sh` 重跑后 `git diff` 应为空）；不引入 GDScript。

## 2 取舍顺序

规则冲突时按此裁决，顺序本身就是纪律：

1. **正确性**——真实路径跑得通、可重复验证。
2. **回归面**——留下的自动判定，胜过一次性人工过目。
3. **一致性**——命名、术语、结构统一。
4. **卫生**——散文形态。它不产生质量信号。

三条裁决例：

- 门禁与正确性冲突 → 改门禁（例：门禁判不出你要做的事，先改门禁的判据，不是绕开它）。
- 样式一致与可测性冲突 → 让位可测性（例：把判定挪进 `core/` 会打破「就近放置」，仍然挪）。
- 确实无法自动化 → 不降低标准，而是**登记人工项 + 留代码层护栏**（见 §5）。

## 3 决策权

**可以自主做**：

- 按既有规格实现功能、修 bug、修与规格不符的实现；
- 行为不变的重构、命名与目录整理、删孤儿代码；
- 测试、注释、文档结构；
- 性能优化（不改变观感、难度与玩家可感数值）；
- 让门禁真正能失败（修假绿）。

**只能提议、不许擅自定**（属于人）：

- `balance.json` 里玩家可感的取值：伤害、血量、速度、冷却、概率、奖励、手感系数；
- 难度曲线、手感、节奏；
- 新增或删除玩家可见内容与文案；
- 新增设置项、改存档格式、改输入面；
- 平台范围、第三方依赖、发布。

提议必须给成对信息，落地时在提交正文标注「该取值待人类确认」：

```
结论：建议 <取值>（默认已按它落地，待确认）
| 选项 | 值 | 实测 / 依据 | 代价 |
```

人类没答复时：保留默认值，并在 `docs/ROADMAP.md` 开放项写一行
「待确认：<取值>；收口条件＝人类认可或改值」。不得把未确认的取值当成既成事实写进
`DESIGN_BASELINE.md`。

判据速记：**玩家能感觉到、且没有客观对错的，归人**；能靠实测与规格判定的，归实现者。

## 4 完成的定义

通用清单（每次改动逐条过）：

1. `python3 scripts/ci/gates.py` 全绿，并在交付说明里给出命令与实际结果；
2. 行为改动有**能失败的**回归面，或按 §5 登记人工项 + 护栏；
3. 涉及数值 / 文案 / 存档 / 设置 → 对应单源已改，全库无第二处硬编码；
4. 新增数值键在 `balance.json` 有值，且与代码内默认值一致；
5. 删改的符号没有悬空引用（`grep` 旧名确认）；
6. 提交按 §7 拆分与撰写；本次未做完的部分登记为开放项（§8），不留口头欠账。

分类型附加要求：

| 改动类型 | 额外必做 |
| --- | --- |
| `csharp/core/` 纯逻辑 | 新增/改单测（先红后绿） |
| 引擎耦合行为（节点 / 事件 / 输入 / 结算 / 生成） | 相关无头场景覆盖该路径并断完成标记；判定可下沉的必须下沉 `core/` 并配单测 |
| 视觉 / 着色器 / UI 布局 | 窗口化人工过目（登记人工项，写清怎么算过）；无头只保证不崩 |
| 数值调参 | 键存在 + 取值路径冒烟；玩家可感的手感值另登记人工项 |
| 新增/改动设置项 | `check_settings_symmetry.sh`（写读对称 + 文案键）+ 设置页开页冒烟；改默认值或做字段迁移另做窗口化人工过目 |
| 存档字段 | `check_save_symmetry.sh` + `csharp/tests` 补一条「旧档缺新字段可读入且不丢已存进度」的用例 |
| 玩家可见文案 | `check_ui_copy.sh` + 翻译表键存在 |
| 遭遇事件 / 波次 | 对应 `--event-probe` 全周期 + 完成标记 |
| 性能优化 | 无头同场景同帧数的墙钟用时前后对比（粗筛；观感退化仍需窗口化过目），退化要写明取舍 |
| 依赖 / 工具链 | 先按 §3 提议；零第三方依赖是硬约束 |

## 5 测试策略

**选层判据**（自上而下选第一个可行的）：

1. 能写成不依赖 Godot 的纯函数 / 值对象（几何、时序、经济、概率、整形、判型）→ `csharp/core/` 单测；
2. 必须依赖引擎生命周期 / 物理 / 场景树 → 无头固定步长场景（`--event-probe` 这类探针）；
3. 依赖 GPU 表现 / 手感 / 长时稳定性 → 人工过目，并登记人工项。

Godot 节点只做适配（取节点、设属性、连信号）：**决策下沉 `core/`，节点不藏判定**。
不为下沉而下沉——纯适配留在 `godot/` 层是正常的。

**确定性硬规则**（无头场景与探针一律遵守）：

1. 不等待真实时间：一律 `--fixed-fps 60`（帧数＝模拟时长，机器多快跑多快）；
2. 判定只看模拟状态，不看墙钟、帧率、机器性能；
3. 随机要么避免、要么走可注入的取值源；不得依赖 `GD.Rand*` 的默认序列；
4. 不依赖窗口 / 分辨率 / GPU；
5. 不依赖外部残留状态（存档先复位，或用临时目录）。

**完成信号**：无头场景跑完必须打固定标记（如 `[event-probe] <id> 全周期完成`），门禁断言它。
只判「不崩」不算覆盖——帧数不够时事件中途停摆同样是零错误退出。

**不能自动化时**：登记人工项（§8 的格式，写清「怎么算过」）+ 加代码层护栏
（断言、日志、取值钳制），并在人工项里说明为什么不能自动化。

**顺序与诚实**：先写能失败的用例，再改实现；修 bug 先写复现用例。
新增用例要能说清「实现坏了它怎么红」，说不清就删掉。不稳定测试要么修成确定性，要么删——
不许用重试掩盖。

## 6 门禁

本地一把跑完：`python3 scripts/ci/gates.py`（只做调度，判定逻辑在各自脚本里，不复制口径）；
`--only <slug>` 只跑子集，`--list` 列出步骤。

**质量门禁**（判静默错误，是完成依据）：

| 命令 | 判什么 |
| --- | --- |
| `dotnet build` | 零警告（TreatWarningsAsErrors） |
| `bash scripts/ci/check_unit_tests.sh` | `csharp/core/` 纯逻辑单测 |
| `bash scripts/ci/check_balance_keys.sh` | 代码读的 balance 键真实存在（防键名写错静默回退默认值） |
| `bash scripts/ci/check_save_symmetry.sh` | 存档写读字段一一对应（防进度静默丢失） |
| `bash scripts/ci/check_settings_symmetry.sh` | 设置写读字段一一对应 + 设置页文案键存在 |
| `bash scripts/ci/check_ui_copy.sh` | 玩家可见文案无缺键、无开发措辞、无空字段 |
| `bash scripts/ci/check_import.sh` | 资源导入无警告 |
| `bash scripts/ci/check_smoke.sh` | 无头冒烟四趟：主场景 300 帧 / 设置页开页 / 编队遭遇全周期 / 精英炮塔全周期 |

**卫生门禁**（判散文形态，不产生质量信号）：

| 命令 | 判什么 |
| --- | --- |
| `bash scripts/ci/check_prose_hygiene.sh` | 注释无日期戳、注释散文简体中文、术语单一叫法（与 §9 表同源） |

**铁律**

1. **每个门禁必须能失败**：新增或修改门禁时，在同一提交里给出破坏验证（怎么破坏、红了什么）。
2. **假绿比没门禁更糟**：取不到判据必须显式失败（工具缺失、路径不存在、零命中）。
3. **卫生门禁不得当作完成依据**：它变绿说明不了任何行为正确性；反过来，也不许用「业务优先」
   长期纵容卫生门禁变红——红了就顺手改掉。
4. **时间预算**：本地全跑目标 ≤90 秒（`gates.py` 结束时打印用时）；单条门禁超 30 秒要在上表注明理由。

**新增或修改门禁的准入条件**：能一句话说清「它抓的是哪种静默错误」+ 在 CI 稳定复现 +
失败输出能指向位置 + 有破坏验证。不满足就写成 §4 的检查项或人工项，不要造脚本。

**无头冒烟与探针口径**：

```bash
godot --headless --path . --fixed-fps 60 --quit-after <帧数> -- <开关>
```

- 帧数 = 事件全周期秒数 × 60 + 余量；**帧数只在 `scripts/ci/check_smoke.sh` 维护一份**。
- 开关：`--settings-probe` 开设置页并逐页切过；`--event-probe=formation_strike|elite_turret`
  直进开局并强制触发一次遭遇；`--startup-time` 打印启动分段耗时。
- 事件两趟必须出现 `[event-probe] <id> 全周期完成`。
- 日志：主场景 `<LOG>`、设置页 `<LOG>.settings.log`、编队 `<LOG>.formation.log`、精英 `<LOG>.elite.log`。
- 遭遇事件改动后至少手跑一次对应探针；`Main` 里的探针开关是 ROADMAP 登记的存量偏离（§10）。

## 7 提交与发布

- **粒度**：一个逻辑一个提交。纯格式化、重排、删除独立成提交；纪律与门禁的改动单独成提交，
  便于单独回滚。
- **主题**：≤72 字，`类型: 是什么`；类型限 `feat / fix / refactor / perf / test / docs / chore`。
- **正文**：只写「为什么」与关键取舍，≤3 行；推翻既有决策必须写明。不抄 diff、不复述验证流水、
  不写日期与轮次编号。
- **先落文档再动手**的改动：存档格式、设置键、输入面、事件口径、平台范围、以及本文件本身。
- **提交前**：

  ```bash
  python3 scripts/ci/gates.py          # 全绿
  git status --porcelain               # 只应包含本次改动
  git show --stat HEAD                 # 提交内容与预期一致
  ```

- **发布**：`./release.sh` 导出并打包到 `builds/release/`；`./release.sh --publish` 追加 tag 与
  GitHub Release。发布前 `docs/ROADMAP.md`「发布前人工验收」必须清零，否则不发布。
- **反悔留痕**：推翻既有决策时加一条新决策条目，不改旧条目。

## 8 文档

四份各司其职，只给指针不复述：`README.md`（怎么跑、怎么发）、本文件（纪律与门禁）、
`docs/ROADMAP.md`（方向 + 开放债务 + 人工验收项）、`docs/DESIGN_BASELINE.md`（设计意图定稿）。

条目格式：

- 开放项：`- **[影响面] 是什么**：为什么不现在做；收口条件＝怎么算做完；触发时机＝什么时候做。`
- 人工验收项：`- **名字**：怎么操作、怎么算过；关联代码路径给指针。`
- 决策条目：`- **日期 决策**：为什么（一行）+ 反转链。`
- 活跃文档只写「现状 + 开放项」。根因、修法、验证过程属于 git 历史；修完直接删条目，
  不写「已修复」回顾。

## 9 注释、命名、术语与语言

- 注释只写代码表达不出的东西：为什么这么做、边界条件、坑、外部约束（引擎行为、平台差异）。
- 注释不写：变更史与日期、审计轮次、复述代码的「是什么」、无主 TODO
  （要么写清触发条件与收口，要么删）。
- 命名：类型与公开成员 `PascalCase`、局部与参数 `camelCase`、私有字段 `_camelCase`、
  常量 `PascalCase`；数值键 `snake_case`；文件名与类型同名。
- 术语单一叫法，新术语先写进下表再用（下表是 `check_prose_hygiene.sh` 的机器副本，**改表必须同步脚本**）：

  | 用 | 不用 |
  | --- | --- |
  | 本局（合法复合词：本局内） | 对局 / 单局 / 局内 |
  | 敌机 / 敌弹 | 敌人 / 弹丸 |
  | 增幅 | buff / 增益 |
  | 天赋 | 技能树 |
  | 弹反 | 格挡 |
  | 通用弹写「弹体」，按阵营写「玩家弹 / 母舰弹」 | 弹丸（语义分裂，勿用） |
  | 键鼠 / 手柄（仅有的两路输入面） | 触屏 / 触控 / 移动端（已在代码层退役，回归须先推翻决策） |
  | 黎明站（地点专名）/ 基地（功能语境） | — |

- 语言：散文（注释 / 文档 / 提交信息）一律简体中文；标识符、类型名、引擎 API、配置路径、
  动作名、外部产品名保留英文。
- 玩家文案：只从 `data/translations.csv` 出，经 `check_ui_copy.sh` 判定；不出现开发措辞与占位符。

## 10 已知偏离与例外

偏离纪律的做法必须登记在 `docs/ROADMAP.md`「已知债务与开放发现」（格式：是什么 /
为什么不现在做 / 收口条件 / 触发时机），口头约定不算。需要登记的偏离类型：

- 测试或探测设施进生产路径（违反 §5）；
- 门禁判据与其声称的不符，或长期跑不起来（违反 §6）；
- 用人工项替代本可自动化的回归面（违反 §4、§5）；
- 绕过单源（同一事实两处维护）；
- 跨面改动没先落文档（违反 §7）。

本节不列表条目——现状一律以 ROADMAP 为准，避免同一事实两处维护。

## 维护本文件

改纪律 = 改本文件 + 同步受影响的门禁脚本 + 同一个提交落地。新增规则必须能落到 §4 的清单或
§6 的门禁上；落不到判定上的规则不要写进来。
