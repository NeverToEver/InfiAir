# AGENTS.md — 工程纪律

工程质量与流程的唯一判定口径。做什么见 `README.md`，方向与开放项见 `docs/ROADMAP.md`，
设计意图见 `docs/DESIGN_BASELINE.md`——本文件只写纪律，不复述它们的内容。

参照的通行做法：测试金字塔与 humble object、Conventional Commits、ADR、trunk-based、
零警告构建、确定性 CI。取用它们是为了让判定可重复，不是为了仪式。

## 1 项目形态

- Godot 4.6 .NET + C#（零 GDScript），GL Compatibility，1920×1080；平台 PC 桌面，
  输入面只有键鼠与手柄两路。
- 分层：`csharp/core/` 纯逻辑（零 Godot 依赖，可单测）；`csharp/godot/` 引擎适配层
  （场景脚本、UI、服务）；`csharp/tests/` 只测 core——不入主 sln，游戏构建与发布不耦合测试工具链。
- 单源：数值 `data/balance.json`；颜色 `csharp/godot/UITheme.cs`；帧级口径 `FrameCache`。

## 2 取舍顺序

规则冲突时按此裁决，顺序本身就是纪律：

1. **正确性**——真实路径跑得通、可重复验证，胜过一切形式一致。
2. **回归面**——留下的自动判定，胜过一次性人工过目。
3. **一致性**——命名、术语、结构统一。
4. **文风**——写得好看不产生任何信号，排最后。

因此：任何「为了让门禁变绿」而牺牲前三项的做法都是错的；反过来，任何「因为不好写测试」
而跳过验证的做法同样是错的。

## 3 行为纪律

- **行为改动必须带回归面**。没有可重复判定的行为改动视为未完成。引擎耦合行为用无头固定
  步长跑（见 §4）；能被纯逻辑决定的判定下沉到 `csharp/core/` 后用单测钉住——Godot 节点只做
  适配，不藏决策。
- **人工过目只覆盖不可自动化的部分**：GPU/着色器表现、手感、长时稳定性。人工项不得用来
  关闭代码缺陷；「上实机看看」不是缺陷的收口方式。
- **测试与探测设施不进生产路径**：生产代码不为测试读命令行开关、不为测试锁帧率、不为测试
  改行为。无头驱动一律固定步长，保证同一帧数得到同一结果。
- **数值、手感、产品范围属于人**。智能体给方案与数据，不擅自定值；确需取值推进时，在提交
  信息里点明「该取值待人类确认」，并把它做成 `balance.json` 里的可调旋钮。
- **删代码优先于加代码**：孤儿代码、只为「万一」保留的分支直接删（git 有历史）。
- **一个逻辑一个提交**：不把无关清理混进功能改动，也不把功能改动藏在重排里。

## 4 验证门禁（提交前必过）

| 命令 | 判什么 |
| --- | --- |
| `dotnet build` | 零警告（TreatWarningsAsErrors） |
| `bash scripts/ci/check_unit_tests.sh` | `csharp/core/` 纯逻辑单测 |
| `bash scripts/ci/check_balance_keys.sh` | 代码读的 balance 键真实存在（防键名写错静默回退默认值） |
| `bash scripts/ci/check_save_symmetry.sh` | 存档写读字段一一对应（防进度静默丢失） |
| `bash scripts/ci/check_settings_symmetry.sh` | 设置写读字段一一对应 + 设置页文案键存在 |
| `bash scripts/ci/check_ui_copy.sh` | 玩家可见文案无缺键、无开发措辞、无空字段 |
| `bash scripts/ci/check_import.sh` | 资源导入无警告 |
| `bash scripts/ci/check_smoke.sh` | 无头冒烟四趟：主场景 300 帧 / 设置页开页 / 编队遭遇全周期 / 精英炮塔全周期 |
| `./release.sh` | 打包发布（仅发布时需要） |

本地一把跑完：`python3 scripts/ci/gates.py`（只做调度，判定逻辑仍在各自脚本里，不复制口径）。

**门禁铁律**

- **每个门禁必须能失败**：新增或修改门禁要附一次「故意破坏即变红」的实证；做不到就说明它判
  不出东西。
- **假绿比没有门禁更糟**：取不到判据时（工具缺失、路径不存在、扫描零命中）必须显式失败。
- **门禁只判静默错误**：判据必须是「写错了不会报错、只会悄悄坏」的东西（存档丢字段、配置键
  写错、文案缺键、事件状态机跑不完）。文风类规则不设门禁——它把注意力从正确性引到措辞上。

**无头冒烟口径**：`--headless --fixed-fps 60 --quit-after <帧数>`。固定步长让帧数＝模拟时长，
机器多快就跑多快（不锁帧率、不等真实时间）。遭遇事件改动后必须跑对应探针：`--event-probe=<id>`
直进开局并强制触发一次遭遇，跑完打印 `[event-probe] <id> 全周期完成`，冒烟断言这一行——帧数
不够不会报错、只会静默少覆盖，所以帧数按「事件全周期 × 60」留余量。设置项改动同时过
`check_settings_symmetry.sh` 与设置页开页冒烟：写错的表现是「玩家点开设置就崩」或「改了设置
下次启动回到默认」，两者都不编译报错。

## 5 提交与变更

- 主题行 ≤72 字：`类型: 是什么`（feat / fix / refactor / perf / test / docs / chore）。
- 正文只写「为什么」与不明显的取舍，三行以内。不抄 diff、不复述验证过程（那是门禁的职责）、
  不写日期与轮次编号。
- 行为改动的顺序是先补回归面再改实现；跨面改动（存档格式、设置键、输入面、事件口径）先改
  `DESIGN_BASELINE.md` 再动手。
- 反悔要留痕：推翻既有决策时加一条新决策，不改旧条目。

## 6 文档

- 四份各司其职，只给指针不复述：`README.md`（怎么跑、怎么发）、本文件（纪律与门禁）、
  `docs/ROADMAP.md`（方向 + 开放债务 + 人工验收项）、`docs/DESIGN_BASELINE.md`（设计意图定稿）。
- 活文档只写「现状 + 开放项」。根因、修法、验证过程属于 git 历史，不写进文档、注释和提交
  正文的长篇叙述里。
- 开放项一行一条：是什么 + 为什么不现在做 + 什么条件下收口；修完直接删条目，不写「已修复」
  回顾。
- 人工验收项只登记需要真实硬件/时长/手感的部分，并写清「怎么算过」。

## 7 语言与文案

- 散文（注释 / 文档 / 提交信息）一律简体中文；标识符、类型名、引擎 API、配置路径、动作名、
  外部产品名保留英文。
- 术语单一叫法，新术语先写进下表再用：

  | 用 | 不用 |
  | --- | --- |
  | 本局（合法复合词：本局内） | 对局 / 单局 / 局内 |
  | 敌机 / 敌弹 | 敌人 / 弹丸 |
  | 增幅 | buff / 增益 |
  | 天赋 | 技能树 |
  | 弹反 | 格挡 |
  | 通用弹写「弹体」，按阵营写「玩家弹 / 母舰弹」 | 弹丸（语义分裂，勿用） |
  | 键鼠 / 手柄（仅有的两路输入面） | 触屏 / 触控 / 移动端（已在代码层退役，回归须先推翻决策） |
  | 黎明站（地点专名）/ 基地（功能语境） | — |

- 这两条是约定，不设门禁：文风由格式化工具与 review 兜底，写错术语不是缺陷，写坏行为才是。
- 玩家可见文案另有判据（`check_ui_copy.sh`），与文风无关，互不替代。

## 8 改本文件

改纪律就改本文件，并同步受影响的门禁脚本——两者在同一个提交里落地，否则纪律与判定脱节。
