# gdlint 配置（gdtoolkit 4.x，YAML）——GDScript 静态检查
#
# 规则取舍依据（与项目既有约定/设计确认对齐）：
# - disable（设计确认真冲突）：
#   * class-variable-name：C19 设计确认——CONSTANT_CASE 用于可变脚本回退默认值（项目数据模式）
#   * class-definitions-order：项目未采用固定定义顺序
#   * max-returns：项目 match/多分支风格（_build_shot/decide_back_action/cfg 等）——
#     重构为单返回点反降可读性
# - 放宽上限（保持规则存在，适配项目实际规模）：
#   * max-line-length 140：项目长注释风格（gdUnit4 同款基线）
#   * max-public-methods 60：GameState/Player 门面类公开接口多（A1/A7 接口化使然）
#   * max-file-lines 2000：intro_cinematic 导演类 1700+ 行（单一大场景脚本）
# - 保留默认规则聚焦真实问题：unused-argument（未使用参数）、命名规范（snake_case）、
#   语法结构类（expression-not-assigned/unnecessary-pass/comparison-with-itself 等）
#
# 本地运行：虚拟环境 gdlint autoload/ scripts/
# CI 已接入（.github/workflows/ci.yml lint 步骤）；新增禁用/放宽规则需同步本文件与 AGENTS.md

disable:
  - class-variable-name
  - class-definitions-order
  - max-returns
  - max-public-methods
max-line-length: 140
max-file-lines: 2000
