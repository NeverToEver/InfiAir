# CLAUDE.md

InfiAir：2D 纵版射击游戏，Godot 4.6.2 + C#（.NET 8），全量 C#。

**唯一权威提示词是 `AGENTS.md`**（项目概况、验证命令、硬性约定全在里面），改动前先读它。深入参考：`docs/DESIGN_BASELINE.md`（玩法设计数值）、`docs/ROADMAP.md`（方向与已知债务）、`docs/TESTING.md`（测试）。历史行为规格在 `docs/archive/`（只读，不再维护）。
