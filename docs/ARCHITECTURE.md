# Runtime Architecture & Config (ARCHITECTURE)

> On-demand reference for `AGENTS.md`: node tree of `scenes/main.tscn`, per-script duties, stack, key configs, directory roles. **Rules & entry: `AGENTS.md` + `.agents/*`**; test commands: `docs/TESTING.md`.

## Stack

- Engine: Godot 4.6.2 .NET/mono edition (standard → mono since 2026-08-07 for C# interop), `GL Compatibility` on desktop/mobile.
- Language: **C# — full migration completed 2026-08-08 (M1–M7d, zero GDScript)**; `csharp/core` pure .NET class library (data models/pure logic), `csharp/godot` Godot binding layer (all runtime scripts; decision `docs/archive/2026-08-08-csharp-assessment.md`); `scripts/tools/*.py` offline tools (balance editor, doc gen, asset gen; stdlib only — sprite gens need PIL), not runtime deps.
- Assets: `assets/sprites/` PNG, `assets/audio/` WAV, `assets/fonts/NotoSansSC.ttf`.
- Data: `data/balance.json` tunable source (top sections: version/world_scale/player/enemies/elites/boss/hud/spawner/mothership/buffs/milestones/base_task/progression/difficulty/effects/elite_turret_event/formation_strike_event/fog_events/tutorial/dda/meta); canonical Tab JSON since 2026-07-31, maintained by `balance_editor.py`. `data/translations.csv` zh/en source; `.translation` built by Godot import.
- Codebase boundaries (2026-08-08, post-migration): full C# — hot paths (object pools / bullet hell) and scene-binding layers stay pure C# single implementation; `csharp/core` is pure .NET (zero Godot deps), `csharp/godot` is the Godot-side layer; no cross-language access remains. Build/test chain: `dotnet build` (`InfiAir.csproj` Godot.NET.Sdk → `csharp/core` `InfiAir.Core`); pure-logic tests `dotnet test tests-csharp/` (xUnit); scene/integration tests stay `test/*_test.tscn` assertion scenes (`docs/TESTING.md`).

## Key Config Files

| File | Purpose |
| --- | --- |
| `project.godot` | Name, entry scene, only autoload, viewport/stretch, input map, renderer. Edit via Godot editor. |
| `data/balance.json` | All tunables. `boss` holds `phases` (pattern tables/telegraph/P2 params), `enrage.type_*`, `difficulty_scaling` (count/interval/speed tiers). Edit via `balance_editor.py`. |
| `data/translations.csv` | Translation keys + `zh`/`en` source. |
| `.gitignore` | `.godot/`, imported `*.translation`, IDE files, exports (`builds/`; `export_presets.cfg` committed since 2026-07-30). |
| `run.sh`/`run.command`/`run.bat` | Launch wrappers. `run.sh`: .NET-edition-first engine probe (`godot-mono` → `~/.local/bin/godot-mono` → `godot` → `godot4` → `~/.local/bin/godot` → macOS `/Applications/Godot.app`), warn on old version, args passed through (`--editor` etc.). `run.command` (double-click + terminal, aligned with run.sh since 2026-08-02): candidates incl. `/Applications`+`~/Applications` `Godot*.app`, pick 4.6+, pass engine args, no `exec` (keeps window/output on abnormal exit). |
| `export_presets.cfg` | Linux/X11 + Windows Desktop presets (embedded pck, x86_64); needs matching export templates installed. |
| `release.sh` | Import → dual-platform export → package into `builds/release/` (`VERSION` env sets version). Requires `InfiAir.sln` at root (.NET export hard dep); export log ERROR or missing `data_InfiAir_<platform>_x86_64/` managed-runtime dir = hard fail (2026-08-12); packages ship exe + runtime dir + install scripts. |

No `package.json`/`pyproject.toml`/`requirements*`/`Cargo.toml`/`go.mod`/Makefile/Docker. Packaging resumed 2026-07-30: `export_presets.cfg` committed, `release.sh` one-shot, `packaging/linux/` (user-space install.sh/uninstall.sh[--purge]/infiair.desktop) + `packaging/windows/` (per-user install.bat/uninstall.bat[/purge], Start-menu shortcut). No third-party deps for routine changes.

**Entry (2026-08-04)**: `project.godot` `run/main_scene = res://scenes/welcome.tscn` — accounts (UserDB: PBKDF2 users.json, per-user saves/settings, local leaderboard) + StartPanel retired (merged into welcome). GameOver「回主菜单」and tutorial exit return to welcome. Per-user save path `user://savegame_<user>_<sha256[:12]>.json` (see `docs/archive/2026-08-04-local-accounts-plan.md`).

**Release/CI status (2026-08-02; CI chain refreshed 2026-08-09)**: export templates installed (`~/Library/Application Support/Godot/export_templates/4.6.2.stable/`), `release.sh` proven, artifacts `builds/release/InfiAir-<ver>-linux-x86_64.tar.gz` / `-windows-x86_64.zip` (embedded pck + install scripts, gitignored). **Distributed as GitHub Releases attachments (not in repo)**: `gh release create v<ver> builds/release/InfiAir-<ver>-*.{tar.gz,zip}`. macOS can't run Linux/Windows binaries — platform validation needed on those hosts. CI `.github/workflows/ci.yml` (.NET/mono): official `dotnet-install.sh` (.NET 8 SDK) → `dotnet build` + `dotnet test tests-csharp/` (warnings-as-errors, xUnit) → `dotnet format` ×3 zero-diff gate → official Godot 4.6.2 .NET/mono binary (Linux x86_64, official Release) → zero-GDScript gate (any `.gd` fails) → import warning gate → BALANCE_MAP generator zero-diff gate → headless import + main smoke + compile probe (every `test/*.tscn`) + all assertion scenes (authoritative count: `docs/TESTING.md`; engine error-log scan + scene-count hard check, flake retry once), push/PR; green = merge gate (see `CONTRIBUTING.md`). CD `.github/workflows/release.yml` (manual): install Godot .NET/mono + templates → `dotnet build` → smoke → `release.sh` → tag `v<ver>` → GitHub Release with attachments; input version syncs `project.godot` `config/version`.

## Main Node Tree

```text
Main (csharp/godot/Main.cs)
├─ Starfield / Camera2D
├─ Player
├─ Spawner
├─ BulletPool / EnemyPool
├─ HUD (layer=2; layer=1 freed for MetaHealthFX "above world, below HUD")
├─ BuffUI / PauseUI / SettingsUI / GameOverUI / BaseUI
├─ ExitConfirm
├─ BackNavigator
├─ MouseTrap (mouse-in-window lock, always-on process)
├─ VirtualControls (runtime _Ready, touch input layer; enabled by Settings touch switch, `GameState.TouchControls`)
├─ MetaHealthFX (runtime-created in _Ready, layer=1, fullscreen health/hit FX)
├─ AimFrameLayer (runtime _Ready, world coords, aim-assist brackets, registered `GameState.AimFrameLayer`)
├─ IntroCinematic (runtime on new game, layer=35)
├─ ReturnCinematic (runtime on return, layer=35)
├─ OrbitalStrike (runtime on continue sortie, layer=24)
├─ MothershipSummonWindow (runtime on summon complete, layer=24)
├─ WarpGate (runtime at mothership dock point, world coords)
├─ EliteTurretEvent (runtime _Ready, registered to unified event manager, mutex)
└─ FormationStrikeEvent (runtime _Ready, registered to unified event manager, lowest priority)
```
`scenes/` holds main/player/enemy/boss/bullet/mothership/turret/cinematics/tutorial/welcome; same-name behavior scripts in `csharp/godot/`. All dynamic run entities attach under Main (clear logic + test traversal).
FogEventManager is **not** under Main: it's a service child of the `GameState` autoload (global singleton, `GameState.FogEvents`), holding its own effect layers (fake-enemy container, confusion overlay CanvasLayer, event banner). The **unified event manager** `GameEventManager` (`GameState.Events`, child of GameState, `docs/EVENT_MANAGER.md`, 2026-08-05) batch-manages all random events (fog 4 + encounters 2): one `EVENT_FACTORIES` registry, grouped concurrency (`fog` ‖ `encounter`), unified trigger policy, unified lifecycle + `EventStarted/Ended` signals; fog lifecycle is driven through it (FogEventManager = fog effects layer/API facade), encounter trigger moved out of spawner.

## Per-Script Duties (主脚本,非穷举)

- `csharp/godot/Main.cs`: run orchestration — spawns, milestones, boss, mothership summon, return, give-up, BGM, page flow, entry animations (`StartEntrySequence`); `_Ready` creates & registers `VirtualControls` (touch input layer, `GameState.VirtualControls` + `TouchControlsChanged` enable link, 2026-08-07).
- `csharp/godot/GameState.cs`: global score/HP/buffs/difficulty/RP/tasks/routes/settings/signals facade. Delegates to `BalanceService`/`SaveManager`/`SfxPlayer`/`EntityManager`/`FogEventManager`/`GameEventManager`/`UserDB`/`ProgressionInterop` (8 services; Y 系列 2026-08-09 补 ProgressionInterop 口径); public API forwarded (typed, PascalCase). **GameState 拆域收官 (2026-08-11)**: 原 9 个域 partial 中 8 个域迁为独立服务（`MetaService`/`MissionsService`/`ScoreService`/`RunProgressionService`/`CombatStateService`/`SettingsService`/`InputBindingsService`/`UserSessionService`，RefCounted + 构造注入依赖 + 信号 C# 事件 + GameState 订阅重发；类名=文件名），GameState 收敛为编排门面（组合 8 服务 + 信号重发 + Cfg 中心 + 实体注册表）；唯一 autoload 约束保持；`GameState.Constants/Meta/Missions/Settings/Input/Users.cs` 等 partial 降为门面转发。**局外成长/科技树 (2026-08-09, `docs/archive/2026-08-09-meta-progression-plan.md`)**: `GameState.Meta.cs` — TechPoints 结算（`SettleRun` 死亡唯一入口；战斗退出/返航不结算） + `ApplyMetaLoadout` 新开局预置 buff 层数（`Main.ApplyNewRun` 调用，tutorial/continue 路径跳过），消费于 ResearchLab UI。Also: **gamepad default binding at runtime** (`BindJoypadDefaults()`: left stick/action keys/right-stick aim via InputMap, P0-1) + gamepad settings (`SetJoyAimSpeed`/`SetJoyDeadzone` setters, deadzone apply, profile persistence). **触屏虚拟操控 (2026-08-07, `docs/archive/2026-08-07-deferred-restart-plan.md` §3)**: `TouchControls` setting (default off, profile-persisted) + `TouchControlsChanged` signal; `SetTouchControls()` persists + broadcasts; `VirtualControls` ref forwards to `EntityManager` (Main mounts the `VirtualControls` node and links the signal). **基地任务轮换 (2026-08-05, `docs/FOG_EVENTS.md` §1)**: `MISSION_POOL` (9 任务 = 3 类 × 3 档) + `TaskPool` 无放回抽取 + RefreshPoints 经济 (`base_task.refresh_cost`/`grant_per_visit`, 存档往返) + 按 `kind` 分发任务进度 (`SetKindProgress`: kill/survive/boss 源不依赖具体 id)。
- `csharp/godot/Player.cs`: WASD, mouse aim, auto-fire, boost, fine move, dash, hit handling. Buff visuals via child `PlayerBuffVisuals` (procedural pods/shield arc/aura/beacon + engine tint), driven by `GameState.BuffsChanged`; visual duties (tail/afterimage pool/body tint/hitbox dot/parry visuals/graze flash) delegated to `PlayerVisuals` (pure C# composition, A8 2026-08-03). **Fairness (2026-08-03, `docs/archive/2026-08-03-combat-fairness-plan.md`)**: graze ring `GrazeArea` (mask=enemy_bullet, `player.graze_radius` no world_scale, once/bullet) + parry (`PlayerParry`: WINDUP/ACTIVE/RECOVER FSM + 3.0s cooldown, `player.parry.*`; disc-shape trigger + radial filter in callback — full 360° circle since 2026-08-10, `arc_deg` keeps sector capability, `Bullet.Reflect()`). Aim assist (impl: `AimCrosshair`/`AimFrameLayer` + `player.aim_assist` tiers): enemies rolled `AimMarked` by `mark_ratio` (0.25); AimFrameLayer draws brackets; AimCrosshair (child of Player, top_level) follows `AimPoint()` and hides system cursor; in-frame `Fire()` writes `Bullet.HomingTarget` (bounded `Bullet.HomingTime`; close = direct, mid = steer, spiral-converge no orbit), out-of-frame = straight fire. `AimPoint()` smooths (in-frame weak stick `stick_factor`, out-of-frame near magnet, cone weak track); shared by aim/fire/crosshair, advanced once per render frame; magnet + weak track share falloff (`player.aim_assist.falloff`: full <400px → linear to 0.3 floor at 1400px).
- `csharp/godot/Spawner.cs`: wave spawning + special-slot scheduling. Normal waves grouped with interval ramp; every 3–4 normal waves an elite wave; elites/encounter events occupy special slots (normal waves paused), Boss scheduled by independent score/time trigger (also pauses waves), rest wave after kill. Waves, boss-3 minions via `GameState.EnemyPool.Spawn()` (2026-08-02; `Spawn`/`Enemy.Reactivate` optional `pBulletType`; `UsePool` const true — the `false` direct-instantiation A/B branch removed with the C# migration); formation crafts/bombs spawn direct (`new FormationCraft()`/`new FormationBomb()` + `QueueFree()` on death, `FormationStrikeEvent.cs`). Encounter event trigger policy moved to `GameEventManager` (2026-08-05); spawner keeps the Boss-freeze/wave-pause mutex hooks (`SetBossFrozen`/`SetWavesPaused`) + `NotifyEventTriggered()` (wave-slot reset).
- `csharp/godot/Enemy.cs`/`Mothership.cs`/`Bullet.cs`/`LaserWeapon.cs`: instantiable combat entities/weapons. `Bullet.cs` fairness (2026-08-03): grace frames (enemy bullet in player Hitbox defers `player.grace_period`, `AreaExited` cancels, single overlaps recheck at expiry), `TryGraze()` once-count, `Reflect()` (faction flip/mirror/×2 speed/×1.5 dmg).
- `csharp/godot/Boss.cs`: phase pattern tables (P1/P2/ENRAGE, `boss.phases.typeN` + telegraph), 4-type enrage (`boss.enrage.type_*`, player slow ×0.35), difficulty tiers × once in `_Ready` (`boss.difficulty_scaling`). `FIGHT_Y` = offset from view top; usages via `FightAnchorY()`. **Transition fairness (2026-08-03)**: P1→P2 & ENRAGE clear all bullets + brief invincibility (`boss.phases.clear_on_shift`/`transition_invincible`; escape phases exempt). Design: `docs/BOSS_REDESIGN.md`.
- `csharp/godot/BulletPool.cs`/`EnemyPool.cs`/`Explosion.cs`/`Starfield.cs`/`CameraShake.cs`/`SpawnTelegraph.cs`: pooling + presentation.
- `csharp/godot/Hud.cs`/`BuffSelect.cs`/`BaseConsole.cs`/`SettingsUi.cs`/`PauseUi.cs`/`GameOverUi.cs`/`ExitConfirm.cs` + `csharp/godot/ResearchLab.cs`: pages/overlays. `Hud` `SegmentedBar` (2026-08-03: `seg_weights`/`seg_colors`; boss bar per-phase colors; AB22/AC23: `hud.boss_bar_segments` key removed — segment count derives from the weight array) + `ParryBar` node (a `SegmentedBar` instance, `Player.ParryEnergyRatio()`). Entry screen `csharp/godot/Welcome.cs` (`scenes/welcome.tscn`, 2026-08-04): brand zone + account panel (login/register/guest/delete + username dropdown) + difficulty + tutorial + settings + local leaderboard + quit; exit confirm self-handled. **研究实验室 (ResearchLab.cs, 2026-08-09, 局外成长)**: TechPoints 消费 overlay — Welcome 主菜单 + BaseConsole 第五面板双入口, `meta.upgrades` 升级购买 (`GameState.Meta`)。`start_panel.gd`/`start_radar.gd` retired (StartPanel merged into welcome; `StartBackdrop` decor reused by welcome). `BaseConsole` missions panel (2026-08-05): renders `GameState.ActiveMissionIds()` (轮换后非固定 `MISSION_DEFS`) + 刷新任务按钮（点数不足禁用并提示，`BASE_NO_REFRESH_POINTS`）；`ShowBase()` 发放 `GRANT_PER_VISIT` 刷新点。
- `csharp/godot/EntityManager.cs` (`EntityManager`, `docs/ENTITY_MANAGER.md`, 2026-08-05 evolved from EntityRegistry): unified entity manager (RefCounted service child of GameState) — `Enemies` + O(1) `HasEnemy` hot path, `EnemyBullets` (death-replay source), special refs (`PlayerRef`/`PlayerHitbox`/`BulletPool`/`EnemyPool`/`AimFrameLayer`/`CameraRef`/`VirtualControls`); one-line registration `BindEnemy`/`UnbindEnemy` (group + register + `EntityRegistered`/`EntityUnregistered` signals, replacing the 4-class boilerplate); bulk APIs `ForEachEnemy`/`ClearEnemies`/`CountEnemies` (predicate-filtered, invalid-instance-safe) live on the GameState facade over the registry — spawner spread count uses `CountEnemies`; main orbital clear, mothership targeting/slow field and enrage volley iterate `GameState.Enemies` directly (C# port: Callable predicates have no Func overload on those paths). Pooling unchanged: `EnemyPool`/`BulletPool`/`Explosion` (low-frequency units stay direct-spawn).
- `csharp/godot/IDamageable.cs` / `ISlowable.cs` (2026-08-16): combat extensibility contracts — `EntityDamage.Dispatch` sends damage to any `IDamageable`; mothership summon slow applies to any `ISlowable`. Enemy/Boss implement both; TurretBattery/FormationCraft implement `IDamageable`. New unit types no longer require dispatcher edits.
- `csharp/godot/GameEventManager.cs` + `csharp/godot/FogEventManager.cs` + `csharp/godot/GameEvent.cs`（通用事件基底）/`FogEvent.cs`（迷雾专门化层）/`FakeEnemy.cs`/`FakeEnemiesEvent.cs`/`ConfusionEvent.cs`/`BulletMalfunctionEvent.cs`/`DirectionShiftEvent.cs`: unified random-event system (`docs/EVENT_MANAGER.md` + `docs/FOG_EVENTS.md`) — **`GameEventManager`** (child of GameState, `GameState.Events`): one `EVENT_FACTORIES` registry (fog 4 + encounters 2), grouped concurrency (`fog` ‖ `encounter`), unified trigger policy (`fog_events.*` / `elite_turret_event.trigger_*` / `formation_strike_event.trigger_*`, balance keys unchanged), unified lifecycle (fog `GameEvent` start/tick/end + duration; encounters Node FSM + `is_active` poll) + signals `EventStarted/Ended`; encounter gate = injected spawner processing (`IsProcessing()`+`CanProcess()`). **`FogEventManager`** = fog effects layer/API facade (visual base: fake-enemy container/overlay/banner; re-emits `FogEventStarted`/`FogEventEnded` + `FogDirectionShift`; config vars + lifecycle forward to `GameState.Events` fog group). Event interface (2026-08-05): **`GameEvent`（纯生命周期接口，零系统耦合）→ `FogEvent`（context 访问器）→ 具体事件**。`csharp/godot/TaskPool.cs`: 任务池无放回抽取壳（洗牌游标；转发 `TaskPoolInterop`，算法核心 `csharp/core/Missions/TaskPool.cs`；`docs/FOG_EVENTS.md` §1）。
- `csharp/godot/MetaHealthFX.cs` + `assets/shaders/meta_health.gdshader` + `crack_field_bake.gdshader`: fullscreen health/hit FX (design `docs/META_HUD_DESIGN.md`) — hit CA/radial blur, directional ripple, low-HP crack growth (Voronoi baked once: windowed GPU 512² / headless CPU 64², **paths must stay equivalent**), desaturate/vignette, DYING heartbeat/breath/HUD shake; full-HP: ColorRect hidden + `_Process` early-out (zero GPU ≈, ≈zero CPU). Values `effects.meta_health`; "reduce flash" in Settings.
- `csharp/godot/CinematicFx.cs` (CinematicFx): shared FX static factory — `SoftGlow`, textured `Particles` (≤96/emitter, `ParticleAmountCap`), double shockwave ring (`Shockwave`), layered `Beam`, `RadialStreaks` (`speed_lines` removed 2026-08-03, zero consumers), plus composition primitives `Glow`/`RectPoly`/`BgRect`/`Line` (2026-08-12 single-source: intro/return private copies merged here, each director keeps one-line forwards); drive `_Process` zero heap alloc. Shared by intro/return/mothership shows.
- `csharp/godot/FrameCache.cs` (FrameCache, 2026-08-11): per-physics-frame shared cache — `ViewRect()`/`Player()`/`DdaFactor()` 一次性查询供全场上百实体/子弹复用（替代 10 处同构每帧私有缓存样板）; static cache holds value types only (U07). `csharp/godot/CfgFx.cs` (CfgFx, 2026-08-11): typed config reads `Float/Int(path, def, min, max)` — AC4 判型口径 + 集中钳制（PathResolver 坏类型回退为第一道防线，判型为第二道），终结 Cfg 直读/判型双口径并存。`csharp/godot/FlashFx.cs` (FlashFx, 2026-08-11): hit-flash 收敛 `Hit/Update`（Enemy/Boss/TurretBattery/FormationCraft 4 处受击闪白同构）。`csharp/godot/EnemyFx.cs` (EnemyFx, 2026-08-11): `RampCollisionDamage` 体碰伤害公式单源（Enemy/Boss 两处）。`csharp/godot/BuffBoolCache.cs` (BuffBoolCache, 2026-08-11): buff 布尔缓存 id 驱动/信号事件驱动/零闭包（Enemy/Boss slow_field、LaserWeapon laser_beam 3 处同构）。`csharp/godot/GlowDot.cs` (GlowDot, 2026-08-12): 辉光圆点单类（IntroGlowDot 并入，`GlowDot` 属性写法保留）。
- `csharp/godot/IntroCinematic.cs`: intro director (6 shots, new game, `docs/INTRO_CINEMATIC.md`); tree paused, Esc via BackNavigator → `Main.SkipIntro()`, any key/click skips; both paths → `Finished` resume.
- `csharp/godot/ReturnCinematic.cs` + `csharp/godot/DawnStation.cs`: return director (7 shots, hold B, `docs/RETURN_HOME_CINEMATIC.md`); mirrors intro, Esc → `Main.SkipReturn()`, both paths → base UI (tree stays paused; BGM −40dB in shot 7). `DawnStation` shared factory (destroyed/hologram states; intro shot 1, return shots 2/3/4, base backdrop).
- `csharp/godot/OrbitalStrike.cs`: continue-sortie clear anim: sights → missiles → impact pillar/rings; tree paused; `Struck` signal drives main-registry clear (Boss kept, per-ship explosions) + resume; values `effects.orbital_strike`.
- `csharp/godot/MothershipSummonWindow.cs` + `csharp/godot/WarpGate.cs`: summon show (no pause; input-locked + event invincibility). Charge: main overlays `_chargeFx` (dual shrinking rings/inward particles/backlight); hanger window (pipeline off → arm release → launch + shuttle, layer=24, unified `Finished`, `Skip()` for tests); then warp gate (soft core + inner swirl + inward particles + lip occluder); mothership DESCEND decelerates (`warp_in_drop`), dual-ring slow zone (`Enemy`/`Boss.ApplySlow`), fire cover; DOCKING tows player to pod (`Player.EnterPod()` hides + disables hits, RELEASE `Player.ExitPod()`), resupply/loiter/leave. Values `effects.mothership_summon`.
- `csharp/godot/EliteTurretEvent.cs`/`StrikeCarrier.cs`/`TurretBattery.cs` (+ `scenes/turret.tscn`)/`CommOverlay.cs`: turret event (`docs/ELITE_TURRET_EVENT.md`) — event FSM (triggered by `GameEventManager`), Boss mutex (`_bossFrozen`/`_bossPending`/`_wavesPaused` hooks in spawner), carrier director, turret entity (weak lock, `enemy` group + `GameState.Enemies`), comm overlay bottom-left.
- `csharp/godot/FormationStrikeEvent.cs`/`FormationCraft.cs`/`FormationBomb.cs`: formation strike (`docs/FORMATION_STRIKE_EVENT.md`) — lowest-priority random event (triggered by `GameEventManager`; Boss not frozen but **occupies wave slot**, spawner `_wavesPaused`, mutex with turret; `Abort()`-able by return), craft (`enemy` group + registry), fuse bombs (warning ring shrinks with fuse, AoE hits player only).
- `csharp/godot/BackNavigator.cs`: PC Esc / gamepad `ui_cancel` / Android back unified route. Tutorial = standalone `scenes/tutorial.tscn`, self-handles back; aligned with run: `_Ready` creates AimFrameLayer, stage 1 force-marked targets, stage 4 hold-H charge → gate → mothership `BeginWarpIn` → dock (hanger skipped; entity path same as `Main.OnSummonWindowFinished`).
- `csharp/godot/MouseTrap.cs`: mouse-in-window lock (on Main; `GameState.MouseLock`, default on, profile-persisted) — while crosshair active (unpaused + cursor hidden) + window focused, mouse leaving content area warped back inside 1px via `Input.WarpMouse()` (`MouseExited` + per-frame `_Process` defense); released on unfocus/pause/non-crosshair (mouse can leave window, e.g. title-bar close); prevents crosshair freeze/jump from `GetGlobalMousePosition()`; `AimPoint()`/`AimCrosshair` untouched.
- interop 壳 (2026-08-07, 纯 .NET 核心 + Godot 薄壳转发, 公开 API 不变): `BalanceInterop.cs`/`PathResolverInterop.cs`/`SaveStoreInterop.cs`/`UserDbInterop.cs`/`ProgressionInterop.cs` (→ `InfiAir.Core` `Storage`/`Config`/`Progression`; `TaskPoolInterop` 见事件条目); Boss 拆分 4 件: `BossAttacks.cs` (FSM)/`BossFire.cs` (danmaku)/`BossMovement.cs` (+P1 press-down)/`EnrageSequence.cs`; Player 拆分 2 件: `PlayerDamage.cs`/`PlayerDash.cs` (+`PlayerParry.cs`)。

## Directory Roles

| Path | Content |
| --- | --- |
| `autoload/` | Removed with full C# migration (2026-08-08); only autoload = `GameState` (`project.godot`: `*res://csharp/godot/GameState.cs`). |
| `scenes/` | Godot `.tscn` scenes. |
| `scripts/` | Only `tools/` (offline Python); no runtime scripts. |
| `scripts/tools/` | 主要离线工具 (Python stdlib + shell; sprite gens need PIL): `balance_editor.py` (browser partition edit + highlight + server-side structural/type validation + atomic save + auto `.bak` — tune values with it, then minimal test set); `gen_balance_map.py` (regenerates `docs/BALANCE_MAP.md`: all `cfg()` call-site index + json/script dual-write reverse check — run after new/renamed keys); `generate_audio.py` (regenerates committed WAVs); `generate_enemy_sprites.py`/`generate_player_sprite.py`/`generate_mothership_sprite.py` (regenerate unit sprites, PIL, supersample + glow double-layer); `regenerate_all.sh` (统一入口, 依序重跑 4 个素材生成器, 幂等) + `README.md` (工具清单/用法/可复现性). |
| `csharp/core/` | `InfiAir.Core` pure .NET class library (zero Godot deps) — data models/pure logic, e.g. `BalanceModels.cs` (typed model for `data/balance.json`); unit-testable via `tests-csharp/`. |
| `csharp/godot/` | All runtime behavior scripts + test scripts (`csharp/godot/tests/`), namespace `InfiAir` — scene-binding layer + hot paths (pools/bullets) as pure C# single implementation. |
| `tests-csharp/` | xUnit tests for `InfiAir.Core` (ProjectReference → `csharp/core`), run via `dotnet test tests-csharp/`. |
| `assets/` | Sprites, audio/BGM, fonts, shaders (Meta HUD + crack bake). |
| `data/` | Runtime config + translation sources. |
| `test/` | Headless `.tscn + .cs` self-checks, perf bench, autoplay, screenshot tools. |
| `docs/` | EXIT_FLOW, AUDIT_REVIEW_SOP (parallel-audit methodology), ROADMAP, DESIGN_BASELINE, BALANCE_MAP (generated), system design docs, screenshots; `docs/archive/` frozen history (`PORTING_PARITY.md` + `EXECUTION_LOG.md` + `ENDLESS_BALANCE_PLAN.md` + originals). |
| `.agents/` | Agent entry docs linked from `AGENTS.md` (conventions, shell scripts, doc sync). |
| `packaging/` | `linux/` (install.sh/uninstall.sh/infiair.desktop), `windows/` (install.bat/uninstall.bat). |
